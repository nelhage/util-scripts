#!/bin/sh

PWFILE=$HOME/Dropbox/sec/passwd.gpg
what="$1"
echo -n "Password: "
stty -echo
read password
stty echo


(
    gpg --decrypt "$PWFILE"
    echo "$what: $password"
) | gpg --encrypt --armor | sponge "$PWFILE"
